# Admin Claude configuration

Skills and rules here are repository-local mirrors for React/Vite Admin work. Reference patterns come from `admin-client` and `dragonx-employer-web` only through the adaptation policy in `.agent/REFERENCE-SOURCES.md`.
